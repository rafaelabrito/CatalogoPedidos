# .env.example
# Variáveis de Configuração do Banco de Dados (utilizadas pelo docker-compose e appsettings)
DB_NAME=catalogo_pedidos
DB_USER=postgres
DB_PASSWORD=postgres123

# Copie este arquivo para ".env" na raiz do projeto quando precisar sobrepor os valores padrão.